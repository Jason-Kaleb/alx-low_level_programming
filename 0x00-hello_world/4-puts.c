#include <stdio.h>

/**
 *main - function of project (prints sentence wrapped in double quotes)
 *
 * Return: return 0 everytime
 */
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
